class Question {
  String questionText;
  bool questionAnswer;

  // Make it same with lower code!!
  // Question(String q, bool a) {
  //   questionText = q;
  //   questionAnswer = a;
  // }

  Question(this.questionText, this.questionAnswer);
}
